import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const Icon = props => {
  const {name} = props;
  return (
    <View>
      <Text>Icon</Text>
    </View>
  );
};

export default Icon;

const styles = StyleSheet.create({});
